<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="something.companyname.sdktest.MainActivity">

    <EditText
        android:id="@+id/transaction_edit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Transaction amount $"
        android:inputType="numberDecimal"
        android:layout_marginTop="128dp"/>

    <Button
        android:id="@+id/payment_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/transaction_edit"
        android:layout_centerHorizontal="true"
        android:text="Process transaction" />

    <Button
        android:id="@+id/refund_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/payment_button"
        android:layout_centerHorizontal="true"
        android:text="Refund last transaction"/>
</RelativeLayout>